Fix build against hyprutils with explicit CSharedPointer::operator bool().
Matches upstream hyprlock: return !!m_pSeat;

--- a/src/core/Seat.cpp
+++ b/src/core/Seat.cpp
@@ -169,5 +169,5 @@ void CSeatManager::registerCursorShape(SP<CCWpCursorShapeManagerV1> shape) {
 }
 
 bool CSeatManager::registered() {
-    return m_pSeat;
+    return !!m_pSeat;
 }
