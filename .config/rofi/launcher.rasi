/**
 * Frutiger Aero launcher — frosted glass, gel capsules, inner highlights.
 * Palette: matugen → colors.rasi (same derivation as kitty colors.conf).
 **/

/*****----- Configuration -----*****/
configuration {
    modi:                       "combi";
    combi-modi:                 "drun,ssh";
    show-icons:                 true;
    display-combi:              "⌕";
    display-drun:               "⌕";
    display-ssh:                "›";
    drun-display-format:        "{name}";
    /* Wheel follows the row (L→R), not the column. */
    ml-row-up:                  "";
    ml-row-down:                "";
    ml-row-left:                "ScrollUp";
    ml-row-right:               "ScrollDown";
    hover-select:               true;
    me-select-entry:            "";
    me-accept-entry:            "MousePrimary";
}

@import                          "colors.rasi"

* {
    font:                        "Source Sans 3 13";
}

/*****----- Main Window -----*****/
window {
    transparency:                "real";
    location:                    center;
    anchor:                      center;
    fullscreen:                  false;
    width:                       820px;
    x-offset:                    0px;
    y-offset:                    0px;

    enabled:                     true;
    margin:                      0px;
    padding:                     0px;
    border:                      2px 1px 1px 1px;
    border-radius:               28px;
    border-color:                @rim;
    background-color:            @glass;
    background-image:            @gloss-window;
    cursor:                      "default";
}

/*****----- Main Box -----*****/
mainbox {
    enabled:                     true;
    spacing:                     18px;
    margin:                      0px;
    padding:                     22px;
    border:                      0px;
    border-radius:               0px;
    background-color:            transparent;
    children:                    [ "inputbar", "listview" ];
}

/*****----- Inputbar -----*****/
inputbar {
    enabled:                     true;
    spacing:                     12px;
    margin:                      0px;
    padding:                     14px 22px;
    border:                      2px 1px 1px 1px;
    border-radius:               28px;
    border-color:                @rim-input;
    background-color:            @glass-input;
    background-image:            @gloss-input;
    text-color:                  @foreground;
    children:                    [ "prompt", "entry" ];
}

prompt {
    enabled:                     true;
    font:                        "Source Sans 3 16";
    background-color:            transparent;
    text-color:                  @accent;
    vertical-align:              0.5;
}
textbox-prompt-colon {
    enabled:                     true;
    expand:                      false;
    str:                         "::";
    background-color:            transparent;
    text-color:                  inherit;
}
entry {
    enabled:                     true;
    background-color:            transparent;
    text-color:                  @foreground;
    cursor:                      text;
    placeholder:                 "Search";
    placeholder-color:           @muted;
    cursor-color:                @accent;
}

/*****----- Listview -----*****/
listview {
    enabled:                     true;
    columns:                     5;
    lines:                       3;
    cycle:                       true;
    dynamic:                     true;
    scrollbar:                   false;
    flow:                        horizontal;
    layout:                      vertical;
    reverse:                     false;
    fixed-height:                true;
    fixed-columns:               true;

    spacing:                     12px;
    margin:                      0px;
    padding:                     4px;
    border:                      0px;
    border-radius:               0px;
    background-color:            transparent;
    text-color:                  @foreground;
    cursor:                      "default";
}
scrollbar {
    handle-width:                6px;
    handle-color:                @scrollbar;
    border-radius:               6px;
    background-color:            @tile;
}

/*****----- Elements -----*****/
element {
    enabled:                     true;
    spacing:                     10px;
    margin:                      0px;
    padding:                     16px 8px 12px 8px;
    border:                      1px;
    border-radius:               22px;
    border-color:                @rim-tile;
    background-color:            @tile;
    background-image:            @gloss-tile;
    text-color:                  @foreground;
    orientation:                 vertical;
    cursor:                      pointer;
}
element normal.normal,
element alternate.normal {
    background-color:            @tile;
    background-image:            @gloss-tile;
    border-color:                @rim-tile;
    text-color:                  @foreground;
}
element selected.normal {
    background-color:            @gel;
    background-image:            @gloss-gel;
    border:                      3px 1px 1px 1px;
    border-radius:               22px;
    border-color:                @rim-gel;
    text-color:                  @gel-text;
}
element selected.urgent {
    background-color:            @gel-urgent;
    background-image:            @gloss-gel;
    border:                      3px 1px 1px 1px;
    border-radius:               22px;
    border-color:                @rim-gel;
    text-color:                  @gel-urgent-text;
}
element selected.active {
    background-color:            @gel-active;
    background-image:            @gloss-gel;
    border:                      3px 1px 1px 1px;
    border-radius:               22px;
    border-color:                @rim-gel;
    text-color:                  @gel-active-text;
}
element-icon {
    background-color:            transparent;
    text-color:                  inherit;
    size:                        64px;
    cursor:                      inherit;
    horizontal-align:            0.5;
}
element-text {
    background-color:            transparent;
    text-color:                  inherit;
    highlight:                   @text-hl;
    cursor:                      inherit;
    font:                        "Source Sans 3 Medium 11";
    vertical-align:              0.5;
    horizontal-align:            0.5;
}

/*****----- Message -----*****/
error-message {
    padding:                     16px 20px;
    border:                      2px 1px 1px 1px;
    border-radius:               16px;
    border-color:                @rim;
    background-color:            @error-glass;
    background-image:            @gloss-window;
    text-color:                  @on-error-container;
}
textbox {
    background-color:            transparent;
    text-color:                  inherit;
    vertical-align:              0.5;
    horizontal-align:            0.0;
    highlight:                   none;
}
